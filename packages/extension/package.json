{
  "name": "@ziasub/extension",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "bun run build --watch",
    "build": "bun build src/background/index.ts --outdir=public/dist/background && bun build src/content/index.ts --outdir=public/dist/content && bun build src/popup/index.ts --outdir=public/dist/popup && bun build src/options/index.ts --outdir=public/dist/options",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf public/dist"
  },
  "dependencies": {
    "@ziasub/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "typescript": "^5.9.3"
  }
}
